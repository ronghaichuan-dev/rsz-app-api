# 本文件由 /etc/nginx/nginx.conf 默认 server 块中的
# include /etc/nginx/default.d/*.conf; 加载。
# 不应在本文件中再定义 server 块，以免与默认站点冲突。

location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ =404;
}
